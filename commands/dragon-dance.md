Run the baton-pass `dragon-dance` move.

Record a real workflow lesson so future agents do not repeat the same mistake.
Only use this when `foresight` or a session revealed a genuine, reusable problem.
Do not run this by reflex. If nothing was learned, skip it entirely.

Good triggers:
- the baton was stale or missing something important
- a verification claim was misleading (`passed` written when it was not run)
- the receiver had to re-audit too much
- the same workflow failure could clearly repeat

Steps:
1. Append a dragon-dance entry to `docs/progress.md` using the dragon-dance template.
2. If the lesson warrants a permanent rule change, update `docs/agent-handoff.md`.
3. Write only:
   - problem (what caused friction, ambiguity, or rework)
   - impact (what it slowed down, hid, or put at risk)
   - improvement (what process or template change would help)
   - new convention (the rule future agents should follow)

Keep it tight. This is a repair record, not a post-mortem ceremony.

Arguments: $ARGUMENTS
